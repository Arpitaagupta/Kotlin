package conditionals_Loops_Lists

fun main () {
    //QUES
    //Compare the length of two strings, a = “Internshala” and b = “Trainings” and
// print “length is same” if it is same and print “Length is different” if it is different.

    val a = "Internshala"
    val b = "Trainings"

    if (a.length == b.length) {
        println("length is same")
    }

    else {
        println("Length is different")
    }
}